<forms>
    {% if projects %}
        {% for project_name, qid in projects %}
            <formID>{{ project_name }}</formID>
            <form url="{{ base_url }}{% url datawinners.xforms.views.xform qid %}">{{ project_name }}</form>
            <formID>{{ project_name }}</formID>

        {% endfor %}
    {% endif %}
</forms>