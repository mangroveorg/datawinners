<h:html xmlns="http://www.w3.org/2002/xforms" xmlns:h="http://www.w3.org/1999/xhtml"
        xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:xsd="http://www.w3.org/2001/XMLSchema"
        xmlns:jr="http://openrosa.org/javarosa">
    <h:head>
        <h:title>{{ questionnaire.name }}</h:title>
        <model>
            <instance>
                <data>
                    {% for field in questionnaire.fields %}
                        <{{ field.code }}>{{ field.name }}</{{ field.code }}>
{#                    <mystring>Hello World!  This is my answer!</mystring>#}
{#                    <q2/>#}
                    {% endfor %}
                <form_code></form_code>
                </data>
            </instance>
            {% for field in questionnaire.fields %}
                <bind nodeset="/data/{{ field.code }}" type="{{ field.type }}"/>
                <bind nodeset="/data/form_code" type="string" visible="false()">{{ questionnaire.form_code }}</bind>
{#            <bind nodeset="/data/q2" required="true()"/>#}
            {% endfor %}
        </model>
    </h:head>
    <h:body>
        {% for field in questionnaire.fields %}

        <input ref="{{ field.code }}">
            <label>{{ field.name }}</label>
            <hint>{{ field.instruction }}</hint>
        </input>
{#        <input ref="q2">#}
{#            <label>This is another prompt</label>#}
{#            <hint>This prompt is required</hint>#}
{#        </input>#}
        {% endfor %}

    </h:body>
</h:html>